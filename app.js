// npm - global comand, come with node
// npm --version

//local dependency - use it only in prticular project

// npm i <pakagename>

// global pedendency - use it in any project
// npm install -g <packageName>


// sudo npm install -g <packageName> (mac)

// package.json  - manifest file (impostant staff abou project /package)
// how to create it
//manual approach (create package.json in the root folder, create the propreties)
// npm init (step by sptep with questions, pres enter to skip  )
// npm init -y (all by default)